[project]
name = "ai-usage-monitor"
version = "1.1.1"
description = "Local read-only usage monitor for Claude Code and Codex logs."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
ai-usage-monitor = "ai_usage_monitor.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
